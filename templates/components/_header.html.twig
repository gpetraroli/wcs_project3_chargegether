<header class="bg-primary d-flex justify-content-start align-items-center gap-4">
    <div>
        <a href="{{ path(route) }}" class="text-white fs-1 col-1 ps-3"><i class="bi bi-arrow-left-circle-fill"></i></a>
    </div>
    <h4 class="text-white my-3">
        {{ title }}
    </h4>
</header>
